# 📱 UX/UI Документация - Apple Watch App

## 🎯 Цель документа

Этот документ описывает **полный UX/UI** текущего приложения для Apple Watch, чтобы при переписывании с нуля сохранить все визуальные элементы, анимации, переходы и пользовательский опыт.

**ВАЖНО**: Все описанное здесь должно быть точно воспроизведено в новой версии!

---

## 📐 Общая архитектура экранов

### Навигационная структура

```
App Launch
    ↓
MainView (главный экран)
    ├─→ WatchSettingsView (sheet)
    │   └─→ LogViewerView (sheet)
    ├─→ CountdownView (встроенный в MainView)
    └─→ ActiveMeditationView (fullScreenCover)
        └─→ CompletionView (fullScreenCover)
```

---

## 🏠 MainView (Главный экран)

### Визуальный дизайн

**Layout**: `VStack(spacing: 0)`

#### Верхняя часть (Content Area)
- **Spacer()** сверху
- **Эмодзи**: `🧘`
  - Шрифт: `.system(size: 40)`
  - Центрирован
- **Заголовок**: "Медитация"
  - Шрифт: `.system(size: 16, weight: .regular)`
  - Цвет: `.primary`
- **Spacer()** снизу
- **Spacing между элементами**: `4pt`

#### Нижняя часть (Toolbar)
- **Кнопка Play** (центрирована)
  - Круглая кнопка: `Circle().fill(.white)`
  - Размер: `50x50pt`
  - Иконка: `play.fill` (SF Symbols)
  - Размер иконки: `.system(size: 18)`
  - Цвет иконки: `.black`
  - Стиль кнопки: `.plain`
- **Padding снизу**: `8pt`

#### Toolbar (Navigation)
- **Иконка настроек** (слева вверху)
  - Иконка: `gearshape` (SF Symbols)
  - Открывает `WatchSettingsView` через `.sheet`

### Состояния

1. **Idle** (`countdownPhase == -1`)
   - Показывается главный экран с кнопкой Play

2. **Countdown** (`countdownPhase >= 0`)
   - Показывается `countdownView` (встроенный)
   - Navigation bar скрыт: `.navigationBarHidden(true)`

### Анимации и переходы

- **Переход к countdown**: автоматический при нажатии Play
- **Переход к ActiveMeditationView**: через `.fullScreenCover` после завершения countdown

### Интерактивность

- **Кнопка Play**: запускает `startCountdown()`
- **Кнопка Settings**: открывает `WatchSettingsView` через sheet

---

## ⏱️ CountdownView (Встроенный в MainView)

### Визуальный дизайн

**Layout**: `VStack` с `Spacer()` сверху и снизу

#### Отображение

**Phase 0** (`countdownPhase == 0`):
- **Эмодзи**: `🧘`
- Шрифт: `.system(size: 60)`
- Центрирован

**Phase 1-3** (`countdownPhase == 1, 2, 3`):
- **Число**: `4 - countdownPhase` (т.е. "3", "2", "1")
- Шрифт: `.system(size: 80, weight: .light, design: .rounded)`
- Цвет: `.primary`
- Центрирован

### Анимации

- **Transition**: `.scale.combined(with: .opacity)`
- **Анимация смены фаз**: `withAnimation { countdownPhase = newValue }`
- **ID для анимации**: `.id(countdownPhase)` - обеспечивает плавную смену

### Логика

- **Длительность каждой фазы**: 1 секунда
- **Всего фаз**: 4 (0: 🧘, 1: "3", 2: "2", 3: "1")
- **После фазы 3**: автоматический переход к `ActiveMeditationView`

### Тайминг

```
T+0s:  Phase 0 (🧘)
T+1s:  Phase 1 ("3")
T+2s:  Phase 2 ("2")
T+3s:  Phase 3 ("1")
T+4s:  Переход к ActiveMeditationView
```

---

## 🧘 ActiveMeditationView (Экран активной медитации)

### Визуальный дизайн

**Layout**: `VStack(spacing: 16)`

#### Верхняя часть
- **Spacer()**

#### Центральная часть
1. **Таймер** (главный элемент)
   - Формат: `MM:SS` (например, "05:00")
   - Шрифт: `.system(size: 48, weight: .light, design: .rounded)`
   - Моноширинный: `.monospacedDigit()`
   - Центрирован

2. **Пульс** (если доступен, `heartRate > 0`)
   - Layout: `HStack(spacing: 4)`
   - Иконка: `heart.fill` (SF Symbols)
   - Цвет иконки: `.red`
   - Число: `Int(heartRate)`
   - Шрифт числа: `.title2`, `.monospacedDigit()`
   - Текст: "уд/мин"
   - Шрифт текста: `.caption`
   - Цвет текста: `.secondary`

#### Нижняя часть
- **Spacer()**

#### Кнопки управления

**Состояние 1: Обычное** (`!isWaitingForAcknowledgment`)
- Layout: `HStack(spacing: 16)`
- **Кнопка Pause/Resume**:
  - Иконка: `pause.fill` или `play.fill` (в зависимости от `isPaused`)
  - Размер иконки: `.title2`
  - Размер кнопки: `60x60pt`
  - Фон: `Color.primary.opacity(0.2)`
  - Скругление: `cornerRadius(30)`
  - Стиль: `.plain`
- **Кнопка Stop**:
  - Иконка: `xmark`
  - Размер иконки: `.title3`
  - Размер кнопки: `50x50pt`
  - Фон: `Color.primary.opacity(0.1)`
  - Скругление: `cornerRadius(25)`
  - Стиль: `.plain`

**Состояние 2: Ожидание подтверждения** (`isWaitingForAcknowledgment == true`)
- **Кнопка "Завершить"**:
  - Текст: "Завершить"
  - Шрифт: `.headline`
  - Стиль: `.borderedProminent`
  - Занимает всю ширину

### Padding
- Общий: `.padding()` (стандартный padding SwiftUI)

### Навигация
- **Back button**: скрыт (`.navigationBarBackButtonHidden(true)`)
- **Переход к CompletionView**: через `.fullScreenCover(isPresented: $showCompletion)`

### Haptic Feedback

- **При старте**: `WKInterfaceDevice.current().play(.start)`

### Состояния

1. **Активная медитация** (`!isPaused && !isWaitingForAcknowledgment`)
   - Таймер работает
   - Показываются кнопки Pause и Stop

2. **Пауза** (`isPaused`)
   - Таймер остановлен
   - Кнопка Pause меняется на Play

3. **Ожидание подтверждения** (`isWaitingForAcknowledgment`)
   - Таймер завершён
   - Показывается кнопка "Завершить"
   - Smart Alarm активен (повторяющийся haptic)

4. **Завершение** (`showCompletion == true`)
   - Показывается `CompletionView`

---

## ✅ CompletionView (Экран завершения)

### Визуальный дизайн

**Layout**: `ScrollView` с `VStack(spacing: 24)`

#### Элементы (сверху вниз)

1. **Иконка успеха**
   - Иконка: `checkmark.circle.fill` (SF Symbols)
   - Размер: `.system(size: 60)`
   - Цвет: `.green`

2. **Заголовок**
   - Текст: "Медитация\nзавершена" (две строки)
   - Шрифт: `.title3`
   - Вес: `.semibold`
   - Выравнивание: `.center`
   - Многострочный: `.multilineTextAlignment(.center)`

3. **Статистика** (`VStack(spacing: 16)`)
   - **Длительность** (`StatRow`)
     - Иконка: `clock.fill`
     - Метка: "Длительность"
     - Значение: форматированная длительность (например, "5 мин 30 сек")
   
   - **Средний пульс** (если `averageHeartRate > 0`)
     - Иконка: `heart.fill`
     - Метка: "Средний пульс"
     - Значение: "XX уд/мин"
   
   - **Время**
     - Иконка: `calendar`
     - Метка: "Время"
     - Значение: форматированное время (например, "14:30")

4. **Статус синхронизации**
   - **Синхронизация** (`isSyncing == true`):
     - `ProgressView()` (масштаб: `0.8`)
     - Текст: "Синхронизация..."
     - Шрифт: `.caption2`
     - Цвет: `.secondary`
   
   - **Ошибка** (`syncError != nil`):
     - Иконка: `exclamationmark.triangle.fill`
     - Цвет иконки: `.orange`
     - Текст: сообщение об ошибке
     - Шрифт: `.caption2`
     - Цвет: `.secondary`
   
   - **Успех** (по умолчанию):
     - Иконка: `checkmark.circle.fill`
     - Цвет иконки: `.green`
     - Текст: "Сохранено"
     - Шрифт: `.caption2`
     - Цвет: `.secondary`

5. **Кнопка "Готово"**
   - Текст: "Готово"
   - Шрифт: `.headline`
   - Ширина: `.frame(maxWidth: .infinity)`
   - Стиль: `.borderedProminent`
   - Tint: `.green`

### Компонент StatRow

**Layout**: `HStack`

- **Иконка** (слева):
  - SF Symbol из параметра `icon`
  - Цвет: `.secondary`
  - Ширина: `24pt`
  
- **Метка** (слева от Spacer):
  - Текст из параметра `label`
  - Шрифт: `.caption`
  - Цвет: `.secondary`
  
- **Spacer()**
  
- **Значение** (справа):
  - Текст из параметра `value`
  - Шрифт: `.body`
  - Вес: `.semibold`

### Padding
- Общий: `.padding()` (стандартный)
- Вертикальный для статистики: `.padding(.vertical)`

### Форматирование

**Длительность** (`formatDuration`):
- Если есть минуты и секунды: "X мин Y сек"
- Если только минуты: "X мин"
- Если только секунды: "X сек"

**Время** (`formatTime`):
- Формат: `.short` (например, "14:30")

---

## ⚙️ WatchSettingsView (Настройки)

### Визуальный дизайн

**Layout**: `List` с секциями

#### Секция "Длительность"
- **Header**: "Длительность"
- **Picker**:
  - Метка: "Длительность"
  - Опции: `WatchDurationOption.allCases`
  - Отображение: `option.title`
  - Значение: `option.duration`

**Опции длительности**:
- "3 секунды" (3 сек) - для тестирования
- "5 минут" (300 сек)
- "10 минут" (600 сек)
- "15 минут" (900 сек)
- "20 минут" (1200 сек)
- "30 минут" (1800 сек)

#### Секция "Поза по умолчанию"
- **Header**: "Поза по умолчанию"
- **Picker**:
  - Метка: "Поза"
  - Опции: `MeditationPose.allCases`
  - Отображение: `pose.displayName`
  - Значение: `pose`

#### Секция "Debug"
- **Header**: "Debug"
- **Кнопка "Debug Logs"**:
  - Текст: "Debug Logs"
  - Иконка справа: `chevron.right`
  - Цвет иконки: `.secondary`
  - Открывает `LogViewerView` через sheet

### Навигация
- **Title**: "Настройки"
- **Title mode**: `.inline`
- **Открытие**: через `.sheet(isPresented: $showSettings)`

---

## 📋 LogViewerView (Просмотр логов)

### Визуальный дизайн

**Layout**: `ScrollView` с `VStack(alignment: .leading, spacing: 8)`

#### Элементы

1. **Заголовок**
   - Текст: "Debug Logs"
   - Шрифт: `.headline`
   - Padding снизу: `4pt`

2. **Содержимое логов**
   - Текст: содержимое файла логов
   - Шрифт: `.system(size: 10, design: .monospaced)`
   - Выбор текста: `.textSelection(.enabled)` (только для iOS, не watchOS)

### Toolbar
- **Кнопка "Copy"** (справа вверху)
  - На watchOS: показывает alert "Copied!"
  - На iOS: копирует в буфер обмена

### Навигация
- **Title**: "Logs"
- **Открытие**: через `.sheet(isPresented: $showLogs)` из `WatchSettingsView`

### Логика загрузки
- Читает файл: `Documents/countdown_debug.log`
- Показывает последние 1000 строк
- Если файл не найден: "No log file found. Start countdown to generate logs."
- Если файл пуст: "No logs yet. Start countdown to generate logs."

---

## 🎨 Цветовая схема

### Основные цвета
- **Primary**: `.primary` (адаптивный, светлая/тёмная тема)
- **Secondary**: `.secondary` (адаптивный)
- **White**: `.white` (для кнопки Play)
- **Black**: `.black` (для иконки Play)
- **Green**: `.green` (иконка успеха, кнопка "Готово")
- **Red**: `.red` (иконка сердца)
- **Orange**: `.orange` (иконка предупреждения)

### Полупрозрачные цвета
- **Primary 20%**: `Color.primary.opacity(0.2)` (фон кнопки Pause/Resume)
- **Primary 10%**: `Color.primary.opacity(0.1)` (фон кнопки Stop)

---

## 🔤 Типографика

### Размеры шрифтов
- **60pt**: Эмодзи в countdown (Phase 0)
- **48pt**: Таймер в ActiveMeditationView
- **40pt**: Эмодзи на главном экране
- **18pt**: Иконка Play
- **16pt**: Заголовок "Медитация"
- **10pt**: Логи (моноширинный)

### Веса шрифтов
- **Light**: Таймер, числа countdown
- **Regular**: Заголовок "Медитация"
- **Semibold**: Заголовок завершения, значения статистики
- **Headline**: Кнопки "Завершить" и "Готово"

### Дизайн шрифтов
- **Rounded**: Таймер, числа countdown
- **Monospaced**: Таймер, пульс, логи

---

## ✨ Анимации и переходы

### Countdown
- **Transition**: `.scale.combined(with: .opacity)`
- **Анимация смены фаз**: `withAnimation { countdownPhase = newValue }`
- **ID для анимации**: `.id(countdownPhase)`

### Навигация
- **MainView → ActiveMeditationView**: `.fullScreenCover` (полноэкранный переход)
- **MainView → WatchSettingsView**: `.sheet` (модальный снизу)
- **WatchSettingsView → LogViewerView**: `.sheet` (модальный снизу)
- **ActiveMeditationView → CompletionView**: `.fullScreenCover` (полноэкранный переход)

---

## 🎯 Интерактивность

### Кнопки

1. **Play** (MainView)
   - Действие: `startCountdown()`
   - Haptic: нет (но может быть добавлен)

2. **Settings** (MainView)
   - Действие: `showSettings = true`
   - Открывает: `WatchSettingsView`

3. **Pause/Resume** (ActiveMeditationView)
   - Действие: `pauseTimer()` / `resumeTimer()`
   - Визуальная обратная связь: изменение иконки

4. **Stop** (ActiveMeditationView)
   - Действие: `stopTimer()`
   - Если медитация > 3 сек: показывает `CompletionView`
   - Иначе: возврат к `MainView`

5. **Завершить** (ActiveMeditationView)
   - Действие: `acknowledgeMeditationCompletion()`
   - Показывает: `CompletionView`

6. **Готово** (CompletionView)
   - Действие: `dismiss()` + `onDismiss()`
   - Возврат к: `MainView`

### Haptic Feedback

- **Старт медитации**: `.start` (в `ActiveMeditationView.startTimer()`)
- **Завершение медитации**: Smart Alarm с `.notification` каждые 2 секунды (через `notifyUser`)

---

## 📱 Системный UI Smart Alarm

### Визуальный дизайн (системный, не кастомизируемый)

**Элементы**:
- Иконка приложения (из `AppIcon`)
- Название приложения (из `CFBundleDisplayName` или `CFBundleName`)
- Кнопка "Открыть"
- Кнопка "Остановить"

**Кастомизация**:
- ✅ Название: через `CFBundleDisplayName` в `Info.plist`
- ✅ Иконка: через `AppIcon` в `Assets.xcassets`
- ❌ Текст кнопок: системный, не кастомизируется
- ❌ Расположение: системное, не кастомизируется

---

## 🔄 Потоки пользователя

### Поток 1: Успешная медитация

```
MainView (Play)
    ↓
CountdownView (🧘 → 3 → 2 → 1)
    ↓
ActiveMeditationView (таймер работает)
    ↓
Timer завершён → isWaitingForAcknowledgment = true
    ↓
Пользователь нажимает "Завершить"
    ↓
CompletionView (статистика + синхронизация)
    ↓
Пользователь нажимает "Готово"
    ↓
MainView
```

### Поток 2: Медитация через Smart Alarm

```
MainView (Play)
    ↓
CountdownView
    ↓
ActiveMeditationView (таймер работает)
    ↓
Timer завершён → Smart Alarm срабатывает
    ↓
Системный UI Smart Alarm (вибрации каждые 2 сек)
    ↓
Пользователь нажимает "Остановить" на системном UI
    ↓
ActiveMeditationView.onAppear → автоматически показывает CompletionView
    ↓
CompletionView
    ↓
Пользователь нажимает "Готово"
    ↓
MainView
```

### Поток 3: Пауза и возобновление

```
ActiveMeditationView (таймер работает)
    ↓
Пользователь нажимает Pause
    ↓
Таймер останавливается, Smart Alarm отменяется
    ↓
Пользователь нажимает Resume
    ↓
Таймер возобновляется, Smart Alarm перепланируется
    ↓
Продолжение медитации
```

### Поток 4: Досрочное завершение

```
ActiveMeditationView (таймер работает)
    ↓
Пользователь нажимает Stop
    ↓
Если медитация > 3 сек:
    → CompletionView
Иначе:
    → MainView
```

---

## 📐 Размеры и отступы

### Основные размеры
- **Кнопка Play**: 50x50pt
- **Кнопка Pause/Resume**: 60x60pt
- **Кнопка Stop**: 50x50pt
- **Иконка в StatRow**: 24pt ширины

### Отступы
- **MainView toolbar**: `.padding(.bottom, 8)`
- **ActiveMeditationView**: `.padding()` (стандартный)
- **CompletionView**: `.padding()` (стандартный)
- **CompletionView статистика**: `.padding(.vertical)`
- **LogViewerView**: `.padding()` (стандартный)
- **LogViewerView заголовок**: `.padding(.bottom, 4)`

### Spacing
- **MainView content**: `spacing: 4`
- **ActiveMeditationView**: `spacing: 16`
- **CompletionView**: `spacing: 24`
- **CompletionView статистика**: `spacing: 16`
- **Кнопки управления**: `spacing: 16`
- **StatRow элементы**: `spacing: 4` (для пульса)
- **LogViewerView**: `spacing: 8`

---

## 🎭 Состояния UI

### MainView состояния
1. **Idle**: главный экран с кнопкой Play
2. **Countdown**: показывается countdownView

### ActiveMeditationView состояния
1. **Активная медитация**: таймер работает, кнопки Pause/Stop
2. **Пауза**: таймер остановлен, кнопка Play вместо Pause
3. **Ожидание подтверждения**: таймер завершён, кнопка "Завершить"
4. **Завершение**: показывается CompletionView

### CompletionView состояния
1. **Синхронизация**: ProgressView + "Синхронизация..."
2. **Ошибка**: иконка предупреждения + сообщение об ошибке
3. **Успех**: иконка галочки + "Сохранено"

---

## 🔍 Детали реализации

### Countdown логика
- **Timer**: `Timer(timeInterval: 1.0, repeats: true)`
- **RunLoop mode**: `.common` (работает даже при заблокированном экране)
- **Анимация**: `withAnimation { countdownPhase = newValue }`
- **ID для анимации**: `.id(countdownPhase)`

### ActiveMeditationView таймер
- **Timer**: `Timer(timeInterval: 1.0, repeats: true)`
- **RunLoop mode**: `.common`
- **Форматирование времени**: `String(format: "%02d:%02d", minutes, seconds)`

### Smart Alarm
- **Планирование**: в `MainView.startCountdown()` ДО workout session
- **Срабатывание**: автоматически в `endDate`
- **Haptic**: `.notification` каждые 2 секунды через `notifyUser`
- **Отмена**: при паузе, досрочном стопе, или подтверждении завершения

---

## ✅ Чеклист для переписывания

При переписывании с нуля необходимо сохранить:

- [ ] Все размеры шрифтов
- [ ] Все цвета и их opacity
- [ ] Все spacing и padding
- [ ] Все анимации и transitions
- [ ] Все иконки SF Symbols
- [ ] Все состояния UI
- [ ] Все переходы между экранами
- [ ] Все форматы отображения (время, длительность, пульс)
- [ ] Все кнопки и их стили
- [ ] Все haptic feedback
- [ ] Логику countdown (4 фазы, 1 сек каждая)
- [ ] Логику таймера медитации
- [ ] Логику Smart Alarm
- [ ] Системный UI Smart Alarm (название через CFBundleDisplayName)

---

## 📝 Примечания

1. **Монохромная тема**: кнопки используют `Color.primary.opacity()` для адаптивности к светлой/тёмной теме
2. **Моноширинные шрифты**: таймер и пульс используют `.monospacedDigit()` для предотвращения "прыжков" при изменении чисел
3. **Адаптивность**: все цвета используют системные (`.primary`, `.secondary`), кроме специфических (`.green`, `.red`)
4. **Навигация**: используется `NavigationStack` (iOS 16+) и `.fullScreenCover` для полноэкранных переходов
5. **ScrollView**: `CompletionView` использует `ScrollView` для возможности прокрутки на маленьком экране

---

**Дата создания**: 2026-01-08  
**Версия**: 1.0  
**Статус**: ✅ Полная документация текущего UX/UI
