# 🚀 Гайд по настройке проекта в Cursor

## TL;DR
Пошаговые инструкции для автоматической настройки любого проекта в Cursor с готовыми шаблонами и best practices. Выполни все шаги по порядку для оптимальной настройки.

**📅 Дата актуализации с официальной документацией:** 28 декабря 2025  
**📚 Официальная документация:** [cursor.com/docs](https://cursor.com/docs) | [docs.cursor.com](https://docs.cursor.com)  
**📋 Changelog:** [cursor.com/changelog](https://cursor.com/changelog)

---

## 🤖 Что Cursor AI может сделать автоматически

Cursor AI может автоматически выполнить следующие шаги:

1. ✅ Создать структуру `.cursor/`
2. ✅ Создать файл `.cursor/index.mdc` с Project Rules
3. ✅ Получить шаблоны Project Rules с [cursor.directory](https://cursor.directory)
4. ✅ Создать папку `.cursor/notepads/`
5. ✅ Создать файлы Notepads с шаблонами
6. ✅ Создать файл `.cursorignore` с базовыми правилами
7. ✅ Адаптировать шаблоны под конкретный проект

## 👤 Что требует действий пользователя

Следующие шаги требуют действий в UI Cursor:

1. ⚠️ Добавление документации через @Docs (UI) - опционально
2. ⚠️ Настройка MCP серверов (UI) - опционально
3. ⚠️ Выбор режима приватности (UI) - опционально
4. ⚠️ Тестирование функций (ручные тесты) - опционально

---

## ⚡ Быстрая настройка (5 минут)

### Шаг 1: Создание базовой структуры [AI может выполнить]

**Инструкция для Cursor AI:**

1. Создай папку `.cursor/` в корне проекта
2. Для Project Rules используй шаблоны с [cursor.directory](https://cursor.directory):
   - Используй @Web для поиска подходящего шаблона на cursor.directory
   - Если пользователь указал тип проекта (React, Node.js, Python и т.д.), найди соответствующий шаблон
   - Если пользователь не указал тип проекта, спроси у пользователя или используй базовый шаблон ниже
   - Скопируй содержимое найденного шаблона в файл `.cursor/index.mdc`
   - Если шаблон с cursor.directory не найден, используй базовый шаблон ниже как основу

**Базовый шаблон Project Rules (если тип проекта не указан):**

```markdown
# Project Rules

## Общие принципы
- Следуй принципам SOLID
- Создавай маленькие, тестируемые функции
- Разделяй бизнес-логику и представление
- Всегда добавляй типизацию
- Используй современные практики

## Стиль кода
- Именуй переменные в camelCase, классы в PascalCase
- Добавляй JSDoc комментарии для публичных функций
- Используй строгую типизацию
- Следуй принципам DRY, KISS, YAGNI

## Безопасность
- Всегда валидируй входные данные
- Не храни секреты в коде
- Логируй ошибки, но не секреты
- Используй HTTPS для API

## Тестирование
- Создавай тесты для всех функций
- Используй моки для внешних зависимостей
- Стремись к покрытию кода >80%
```

### Шаг 2: Настройка Notepads [AI может выполнить]

**Что такое Notepads:**
Notepads - это инструменты для обмена контекстом в Cursor, которые связывают композеры (Composer) и чат. Они расширяют возможности Project Rules, создавая переиспользуемые контексты для рабочего процесса разработки.

**Инструкция для Cursor AI:**

1. Создай папку `.cursor/notepads/`
2. Создай файл `project-overview.md`:

```markdown
# Обзор проекта

## Технологии
- [Укажи основные технологии проекта]

## Архитектура
- [Опиши архитектуру проекта]

## Стандарты
- Git Flow для ветвления
- ESLint + Prettier для форматирования
- Jest для тестирования
- Conventional Commits для коммитов

## Структура проекта
- [Опиши основную структуру папок]

## API и интеграции
- [Укажи внешние API и сервисы]
```

**Как использовать Notepads:**
- Используй `@ProjectOverview` в чате для ссылки на обзор проекта
- Notepads доступны в Composer и Chat
- Можно создавать несколько Notepads для разных аспектов проекта
- Notepads помогают AI лучше понимать контекст проекта

📚 **Подробнее:** [docs.cursor.com/ru/beta/notepads](https://docs.cursor.com/ru/beta/notepads)

---

### Шаг 3: Добавление документации через @Docs [Требует действий пользователя]

**Важно:** Документацию нужно добавлять через UI Cursor, используя функцию @Docs. Cursor AI не может выполнить этот шаг автоматически.

**Инструкция для пользователя:**

#### 1. Открой Command Palette
- Нажми `⌘+Shift+P`
- Введи "Add new custom docs"
- Выбери команду

#### 2. Добавь источники документации через @Docs

**Основные источники (для всех проектов):**
- **Cursor Docs**: `https://docs.cursor.com/welcome`
- **GitHub**: `https://github.com`
- **Git**: `https://git-scm.com/doc`

**Markdown и документация:**
- **Markdown Guide**: `https://www.markdownguide.org/`
- **GitHub Markdown**: `https://docs.github.com/en/get-started/writing-on-github`

**JavaScript/Node.js (если понадобятся):**
- **MDN Web Docs**: `https://developer.mozilla.org/en-US/docs/Web/JavaScript`
- **Node.js Docs**: `https://nodejs.org/api/`
- **npm Docs**: `https://docs.npmjs.com/`

**Инструменты разработки (если добавим):**
- **ESLint**: `https://eslint.org/docs/`
- **Prettier**: `https://prettier.io/docs/`
- **Jest**: `https://jestjs.io/docs/getting-started`
- **Conventional Commits**: `https://www.conventionalcommits.org/`

**cursor.directory:**
- **cursor.directory**: `https://cursor.directory`
- **API документация**: `https://cursor.directory/api/docs`

#### 3. Использование @Docs в чате

После добавления источников используй в чате:
- `@Docs` - для ссылки на добавленную документацию
- `@Web` - для поиска в интернете
- `@Files` - для ссылки на файлы проекта
- `@Folders` - для ссылки на папки

#### 4. Что НЕ нужно добавлять

Следующие источники НЕ нужны для большинства проектов:
- React Docs (если проект не о React)
- Vue Docs (если проект не о Vue)
- Angular Docs (если проект не о Angular)
- Python Docs (если проект не о Python)
- TypeScript Docs (если не используешь TypeScript)

**Добавляй только релевантные источники для твоего типа проекта!**

### Шаг 4: Настройка MCP интеграции (опционально) [Требует действий пользователя]

**MCP (Model Context Protocol)** позволяет интегрировать внешние системы. Cursor AI не может выполнить этот шаг автоматически.

**Инструкция для пользователя:**

#### 1. Настройка MCP серверов
- Открой настройки Cursor
- Перейди в раздел "MCP Servers"
- Добавь нужные серверы (Confluence, Google Drive, Notion)

#### 2. Использование MCP в чате
- `@MCP` - для доступа к внутренней документации
- Интеграция с корпоративными системами
- Автоматическое обновление контекста

### Шаг 5: Настройка .cursorignore [AI может выполнить]

**Инструкция для Cursor AI:**

Создай файл `.cursorignore` в корне проекта:

```gitignore
# Зависимости
node_modules/
venv/
env/
.env

# Сборка
dist/
build/
out/
.next/
.nuxt/

# Логи
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Временные файлы
.tmp/
.temp/
*.tmp
*.temp

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Тестирование
coverage/
.nyc_output/

# Документация
docs/_build/
site/

# Другие
.cache/
.parcel-cache/
```

### Шаг 6: Проверка настройки [Частично AI может выполнить, частично требует действий пользователя]

**Инструкция для Cursor AI:**

Проверь наличие созданных файлов:
1. ✅ Проверь наличие `.cursor/` папки
2. ✅ Проверь наличие `.cursor/index.mdc` файла
3. ✅ Проверь наличие `.cursor/notepads/` папки
4. ✅ Проверь наличие `.cursorignore` файла

**Инструкция для пользователя (ручные тесты):**

Выполни следующие тесты:

1. **Тест Project Rules:**
   - Открой любой файл
   - Нажми `⌘+K`
   - Введи: "Создай функцию валидации email с TypeScript"
   - Проверь, что код следует правилам проекта

2. **Тест Notepads:**
   - Нажми `⌘+L`
   - Введи: "Используя @ProjectOverview, объясни архитектуру"
   - Проверь использование контекста

3. **Тест документации:**
   - Нажми `⌘+L`
   - Введи: "Согласно @Docs, как настроить Project Rules?"
   - Проверь доступ к документации через @Docs

4. **Тест Agent Mode:**
   - Нажми `⌘+I`
   - Введи: "Создай компонент React с TypeScript и тестами"
   - Проверь автоматизацию

5. **Тест @ символов:**
   - Нажми `⌘+L`
   - Введи: "Используя @Files, покажи структуру проекта"
   - Проверь работу @ символов

6. **Тест MCP интеграции:**
   - Нажми `⌘+L`
   - Введи: "Используя @MCP, найди информацию о проекте"
   - Проверь доступ к MCP серверам

---

## 🔒 Шаг 7: Настройка приватности и безопасности (опционально) [Требует действий пользователя]

**Важно:** Настройка приватности требует действий в UI Cursor. Cursor AI не может выполнить этот шаг автоматически.

### 1. Выбор режима приватности

**Инструкция для пользователя:**

**Открой настройки:**
- Нажми `⌘+,` (Settings)
- Перейди в раздел "Privacy"
- Выбери "Privacy Mode"

**Доступные режимы:**

#### **Share Data** (по умолчанию)
```
Improve Cursor for everyone
```
- Данные используются для улучшения Cursor
- Все функции работают без ограничений
- Рекомендуется для некритичных проектов

#### **Privacy Mode** (рекомендуемый)
```
No training. Code stored for Background Agent and other features.
```
- ✅ Данные **НЕ используются** для обучения
- ✅ Код хранится только для Background Agent и функций
- ✅ Все функции работают
- Оптимальный баланс приватности и функциональности

#### **Privacy Mode (Legacy)** (максимальная приватность)
```
No training and no storage. Background Agent and other features 
that require code storage will be disabled.
```
- ✅ Данные **НЕ используются** для обучения
- ✅ Код **НЕ хранится** на серверах
- ❌ Background Agent отключен
- ❌ Функции, требующие хранения, отключены
- Для работы с конфиденциальным кодом

**Рекомендация:**
- Для большинства проектов: **Privacy Mode**
- Для конфиденциального кода: **Privacy Mode (Legacy)**

---

### 2. Расширенная настройка .cursorignore для безопасности

Добавь в `.cursorignore` конфиденциальные файлы:

```gitignore
# Секреты и конфигурация
.env
.env.local
.env.production
.env.*.local
*.key
*.pem
*.cert
secrets/
config/secrets/

# Приватные данные
private/
confidential/
*.secret
*.credentials

# API ключи и токены
.aws/
.gcp/
.azure/
.ssh/
.gnupg/

# Базы данных с чувствительными данными
*.db
*.sqlite
*.sql
database/backups/

# Логи с чувствительными данными
*.log
logs/
*.log.*
```

---

### 3. Настройка безопасности Agent

**Важные правила безопасности:**

#### 1. ВСЕГДА проверяй команды перед выполнением
- Agent запрашивает подтверждение для команд терминала
- Внимательно читай каждую команду
- Не подтверждай подозрительные команды

#### 2. Опасные команды (требуют особого внимания):
```bash
❌ rm -rf /          # Удаление всех файлов
❌ sudo commands     # Команды с правами администратора
❌ chmod 777         # Небезопасные права доступа
❌ curl | bash       # Выполнение скриптов из интернета
❌ wget | sh         # Выполнение скриптов из интернета
❌ dd if=/dev/zero   # Опасные операции с диском
```

#### 3. Безопасные команды:
```bash
✅ npm install       # Установка зависимостей
✅ git status        # Проверка статуса Git
✅ git log           # Просмотр истории
✅ ls, pwd, cd       # Навигация по файловой системе
✅ npm run build     # Сборка проекта
✅ npm test          # Запуск тестов
```

#### 4. Ограничение доступа к файлам
Используй `.cursorignore` для защиты конфиденциальных данных:
- Секреты и API ключи
- Приватные конфигурационные файлы
- Базы данных с чувствительной информацией
- Логи с персональными данными

---

### 4. Индексация кодовой базы

**Как работает:**
- Cursor создает эмбеддинги (векторные представления) кода
- Исходный код **НЕ хранится** на серверах
- Хранятся только эмбеддинги и метаданные

**Что хранится:**
- ✅ Эмбеддинги (векторы для семантического поиска)
- ✅ Метаданные (хэши файлов)
- ❌ **НЕ хранится** исходный код
- ❌ **НЕ хранятся** имена файлов

**Где хранятся данные:**
- Векторная база данных Turbopuffer
- Серверы Google Cloud (США)

**Как отключить индексацию:**
1. Открой Settings (`⌘+,`)
2. Перейди в "Features"
3. Отключи "Codebase Indexing"

**Когда отключать:**
- Работа с конфиденциальным кодом
- Корпоративные требования безопасности
- Не нужна функция семантического поиска

---

### 5. Провайдеры моделей и Zero Data Retention

**Гарантии безопасности от провайдеров:**

| Провайдер | Zero Data Retention | Обучение на данных |
|-----------|---------------------|-------------------|
| **Anthropic (Claude)** | ✅ Да | ❌ Нет |
| **OpenAI (GPT)** | ✅ Да | ❌ Нет |
| **Google (Gemini)** | ✅ Да | ❌ Нет |
| **xAI (Grok)** | ✅ Да | ❌ Нет |

**Это означает:**
- ✅ Данные **НЕ используются** для обучения моделей
- ✅ Данные **удаляются** после обработки запроса
- ✅ Полная конфиденциальность вашего кода

---

### 6. Сертификации и комплаенс

**Cursor имеет:**
- ✅ **SOC 2 Type II** сертификация
- ✅ **Penetration Testing** - регулярное тестирование на проникновение
- ✅ **Trust Center** - центр доверия с отчетами

---

### 7. Проверка настроек безопасности

**Чек-лист безопасности:**

```markdown
☐ Выбран подходящий режим приватности
☐ Настроен .cursorignore для конфиденциальных файлов
☐ Понимаю, какие команды Agent может выполнять
☐ Знаю, как отключить индексацию (если нужно)
☐ Проверил, что секреты не попадают в контекст
```

**Тест безопасности:**
1. Создай файл `.env` с тестовым секретом
2. Добавь `.env` в `.cursorignore`
3. Попроси Agent показать содержимое проекта
4. Убедись, что `.env` не виден Agent

---

📚 **Подробная документация:**
- [cursor.com/security](https://cursor.com/security) - Обзор безопасности
- [docs.cursor.com/account/agent-security](https://docs.cursor.com/account/agent-security) - Безопасность Agent
- [docs.cursor.com/models](https://docs.cursor.com/models) - Режимы приватности

---

## 📋 Шаблоны для разных типов проектов

**Важно:** Эти шаблоны приведены для справки. Для реальных проектов используй шаблоны с [cursor.directory](https://cursor.directory), так как они актуальнее и полнее.

**Инструкция для Cursor AI:**
1. Используй @Web для поиска подходящего шаблона на cursor.directory
2. Найди шаблон Project Rules для типа проекта пользователя
3. Используй найденный шаблон вместо шаблонов ниже
4. Если подходящего шаблона нет на cursor.directory, используй шаблоны ниже как основу

### React + TypeScript

**Шаблон с cursor.directory:** [Найти на cursor.directory](https://cursor.directory)

**Базовый шаблон (если нет на cursor.directory):**
```markdown
# Project Rules для React + TypeScript

## Архитектура
- Используй функциональные компоненты с TypeScript
- Создавай хуки для сложной логики
- Разделяй бизнес-логику и представление
- Используй Context API для глобального состояния

## Стиль кода
- Именуй компоненты в PascalCase
- Используй camelCase для переменных и функций
- Добавляй типы для всех props и state
- Используй интерфейсы для сложных типов

## Компоненты
- Создавай маленькие, переиспользуемые компоненты
- Используй TypeScript для всех props
- Добавляй defaultProps где необходимо
- Используй React.memo для оптимизации

## Хуки
- Создавай кастомные хуки для логики
- Используй useCallback и useMemo для оптимизации
- Следуй правилам хуков

## Стилизация
- Используй CSS Modules или Styled Components
- Избегай инлайн стилей
- Создавай переменные для цветов и размеров
```

### Node.js + Express

**Шаблон с cursor.directory:** [Найти на cursor.directory](https://cursor.directory)

**Базовый шаблон (если нет на cursor.directory):**
```markdown
# Project Rules для Node.js + Express

## Архитектура
- Используй MVC или Clean Architecture
- Разделяй роуты, контроллеры, сервисы
- Создавай middleware для общей логики
- Используй dependency injection

## Стиль кода
- Используй async/await вместо промисов
- Добавляй типизацию с JSDoc или TypeScript
- Используй константы для магических чисел
- Следуй принципам REST API

## Безопасность
- Валидируй все входные данные
- Используй helmet для безопасности
- Добавляй rate limiting
- Логируй все запросы

## Обработка ошибок
- Создавай централизованную обработку ошибок
- Используй try-catch для async функций
- Возвращай стандартные HTTP коды
- Логируй ошибки с контекстом
```

### Python + FastAPI

**Шаблон с cursor.directory:** [Найти на cursor.directory](https://cursor.directory)

**Базовый шаблон (если нет на cursor.directory):**
```markdown
# Project Rules для Python + FastAPI

## Архитектура
- Используй Pydantic для валидации данных
- Разделяй модели, сервисы, роутеры
- Создавай отдельные модули для логики
- Используй dependency injection

## Стиль кода
- Следуй PEP 8
- Используй type hints везде
- Создавай маленькие функции
- Используй dataclasses для моделей

## API
- Используй Pydantic для схем
- Добавляй документацию с docstrings
- Используй статус коды FastAPI
- Валидируй все входные данные

## Тестирование
- Используй pytest для тестов
- Создавай фикстуры для данных
- Тестируй все эндпоинты
- Используй моки для внешних сервисов
```

### Vue.js

**Шаблон с cursor.directory:** [Найти на cursor.directory](https://cursor.directory)

**Базовый шаблон (если нет на cursor.directory):**
```markdown
# Project Rules для Vue.js

## Архитектура
- Используй Composition API
- Разделяй компоненты и composables
- Создавай переиспользуемые компоненты
- Используй Pinia для состояния

## Стиль кода
- Именуй компоненты в PascalCase
- Используй camelCase для переменных
- Добавляй типизацию с TypeScript
- Используй defineProps и defineEmits

## Компоненты
- Создавай атомарные компоненты
- Используй slots для контента
- Добавляй валидацию props
- Используй provide/inject для глубокой передачи данных

## Стилизация
- Используй CSS Modules или Scoped Styles
- Создавай переменные CSS
- Используй Tailwind CSS если нужно
```

### Angular

**Шаблон с cursor.directory:** [Найти на cursor.directory](https://cursor.directory)

**Базовый шаблон (если нет на cursor.directory):**
```markdown
# Project Rules для Angular

## Архитектура
- Используй Angular CLI для генерации
- Следуй принципам NgModules
- Разделяй компоненты, сервисы, пайпы
- Используй RxJS для реактивности

## Стиль кода
- Используй TypeScript везде
- Следуй Angular Style Guide
- Используй интерфейсы для типов
- Добавляй декораторы правильно

## Компоненты
- Создавай презентационные и контейнерные компоненты
- Используй OnPush стратегию детекции изменений
- Добавляй типизацию для всех inputs/outputs
- Используй async pipe где возможно

## Сервисы
- Создавай сервисы для бизнес-логики
- Используй dependency injection
- Добавляй типизацию для всех методов
- Используй RxJS для асинхронных операций
```

### Python + Django

**Шаблон с cursor.directory:** [Найти на cursor.directory](https://cursor.directory)

**Базовый шаблон (если нет на cursor.directory):**
```markdown
# Project Rules для Django

## Архитектура
- Следуй принципам Django MVT
- Разделяй приложения по функциональности
- Используй Django REST Framework для API
- Создавай отдельные apps для логики

## Стиль кода
- Следуй PEP 8
- Используй type hints
- Создавай маленькие функции
- Используй Django ORM эффективно

## Модели
- Используй verbose_name для полей
- Добавляй help_text для документации
- Создавай методы для бизнес-логики
- Используй Meta класс для настройки

## API
- Используй Django REST Framework
- Создавай сериализаторы для валидации
- Добавляй permissions для безопасности
- Используй ViewSets для CRUD операций
```

### Mobile (React Native)

**Шаблон с cursor.directory:** [Найти на cursor.directory](https://cursor.directory)

**Базовый шаблон (если нет на cursor.directory):**
```markdown
# Project Rules для React Native

## Архитектура
- Используй функциональные компоненты
- Разделяй нативные и JS компоненты
- Создавай переиспользуемые компоненты
- Используй Context API для состояния

## Стиль кода
- Именуй компоненты в PascalCase
- Используй camelCase для переменных
- Добавляй типизацию с TypeScript
- Используй React Native элементы

## Производительность
- Используй FlatList для больших списков
- Оптимизируй изображения
- Используй useCallback и useMemo
- Избегай лишних ре-рендеров

## Навигация
- Используй React Navigation
- Создавай типизированные роуты
- Добавляй экраны загрузки
- Обрабатывай состояния навигации
```

---

## 🔄 Клонирование и переиспользование проекта

### Создание копии настроенного проекта
```bash
# Создай копию проекта
cp -r /path/to/cursor-project /path/to/new-project
cd /path/to/new-project

# Очисти специфичные настройки
rm -rf .cursor/
rm -rf node_modules/ (если есть)
rm package-lock.json (если есть)
```

### Настройка под новые задачи

#### 1. Очистка и переименование
- Переименуй проект в README.md
- Обнови описание в cursor-config.json
- Удали специфичные файлы старого проекта

#### 2. Создание новой структуры .cursor/
```bash
mkdir -p .cursor/notepads
```

#### 3. Создание Project Rules для нового типа проекта
Создай `.cursor/index.mdc` с правилами для твоего нового проекта:

**Для веб-приложения:**
```markdown
# Project Rules для веб-приложения

## Архитектура
- Используй компонентный подход
- Разделяй бизнес-логику и представление
- Создавай переиспользуемые компоненты

## Стиль кода
- Именуй компоненты в PascalCase
- Используй camelCase для переменных
- Добавляй TypeScript типизацию
```

**Для мобильного приложения:**
```markdown
# Project Rules для мобильного приложения

## Архитектура
- Используй навигацию по экранам
- Создавай адаптивные компоненты
- Оптимизируй производительность

## Стиль кода
- Следуй мобильным паттернам
- Используй нативные компоненты
- Добавляй обработку жестов
```

#### 4. Создание Notepads для нового проекта
Создай `.cursor/notepads/project-overview.md`:
```markdown
# Обзор нового проекта

## Технологии
- [Укажи технологии для нового проекта]

## Архитектура
- [Опиши архитектуру нового проекта]

## Цели проекта
- [Опиши цели и задачи]

## Команда
- [Укажи роли в команде]
```

#### 5. Добавление документации через UI Cursor
1. Открой Command Palette (`⌘+Shift+P`)
2. Выбери "Add new custom docs"
3. Добавь источники для нового типа проекта

#### 6. Создание структуры проекта
```bash
# Для веб-приложения
mkdir -p src/components src/pages src/utils src/hooks

# Для мобильного приложения
mkdir -p src/screens src/components src/navigation src/services

# Для API
mkdir -p src/routes src/controllers src/models src/middleware
```

### Примеры быстрого старта

#### Веб-приложение на React
```bash
# Клонируй проект
cp -r cursor-project my-web-app
cd my-web-app

# Очисти настройки
rm -rf .cursor/
rm -rf node_modules/
rm package-lock.json

# Создай новую структуру
mkdir -p .cursor/notepads
mkdir -p src/components src/pages src/hooks

# Создай Project Rules для React
# Создай Notepads для веб-приложения
# Добавь документацию React через UI Cursor
```

#### Мобильное приложение
```bash
# Клонируй проект
cp -r cursor-project my-mobile-app
cd my-mobile-app

# Очисти настройки
rm -rf .cursor/
rm -rf node_modules/
rm package-lock.json

# Создай новую структуру
mkdir -p .cursor/notepads
mkdir -p src/screens src/components src/navigation

# Создай Project Rules для мобильного приложения
# Создай Notepads для мобильного приложения
# Добавь документацию React Native через UI Cursor
```

### Преимущества клонирования
- ✅ Быстрый старт с готовой структурой
- ✅ Сохранение лучших практик
- ✅ Единообразие настроек
- ✅ Экономия времени на настройку

---

## ✅ Расширенные проверки

### Проверка Tab (Автодополнение)
1. Открой файл с кодом
2. Начни писать функцию
3. Проверь, что Cursor предлагает многострочные автодополнения

### Проверка Inline Edit
1. Выдели фрагмент кода
2. Нажми `⌘+K`
3. Опиши изменения на естественном языке
4. Проверь, что код изменился согласно описанию

### Проверка Agent Mode
1. Нажми `⌘+I`
2. Введи: "Создай полную структуру проекта с README, package.json и базовыми файлами"
3. Проверь, что агент создал все необходимые файлы

### Проверка @ символов
1. Нажми `⌘+L`
2. Используй @Files для ссылки на файл
3. Используй @Folders для ссылки на папку
4. Проверь, что контекст правильно подхватывается

### Проверка Rules
1. Создай правило в `.cursor/index.mdc`
2. Попроси Agent создать код
3. Проверь, что код следует заданным правилам

### Проверка MCP интеграции
1. Настрой MCP сервер (если доступен)
2. Используй @MCP в чате
3. Проверь доступ к внутренней документации

### Проверка Max Mode
1. Включи Max Mode в настройках модели
2. Задай сложный вопрос, требующий большого контекста
3. Проверь качество ответа

---

## 🆕 Новые функции версии 2.0+ (28 декабря 2025)

### Multi-Agents
**Запуск до 8 агентов параллельно на одном промпте.**

**Как использовать:**
1. Открой Agent Mode (`⌘+I`)
2. В новом интерфейсе выбери количество агентов (до 8)
3. Каждый агент работает в изолированной копии кодовой базы
4. Используются git worktrees или удаленные машины для предотвращения конфликтов

**Преимущества:**
- Сравнение разных подходов к решению задачи
- Параллельная работа над разными частями проекта
- Автоматическая оценка результатов (Multi-agent Judging)

📚 **Подробнее:** [cursor.com/changelog/2-0](https://cursor.com/changelog/2-0)

---

### Composer
**Первая агентная модель кодирования Cursor - в 4 раза быстрее.**

**Особенности:**
- Frontier модель для работы с Agent
- Оптимизирована для агентного кодирования
- Значительно быстрее аналогично интеллектуальных моделей

**Как использовать:**
1. Выбери модель Composer в настройках Agent
2. Используй для сложных задач, требующих понимания архитектуры
3. Composer может работать с несколькими файлами одновременно

📚 **Подробнее:** [cursor.com/changelog/2-0](https://cursor.com/changelog/2-0)

---

### Plan Mode
**Режим планирования с поддержкой Mermaid диаграмм.**

**Как использовать:**
1. Открой Plan Mode в интерфейсе Agent
2. Создай план с одним моделью, строй с другой
3. Используй интерактивный UI для ответов на уточняющие вопросы
4. Планы поддерживают inline Mermaid диаграммы
5. Используй ⌘+F для поиска внутри планов
6. Отправляй выбранные задачи новым агентам

**Преимущества:**
- Визуализация планов через диаграммы
- Планирование в фоне или с параллельными агентами
- Лучшее качество планов через уточняющие вопросы

📚 **Подробнее:** [cursor.com/changelog/2-0](https://cursor.com/changelog/2-0) | [cursor.com/changelog/2-1](https://cursor.com/changelog/2-1) | [cursor.com/changelog/2-2](https://cursor.com/changelog/2-2)

---

### Browser (GA)
**Встроенный браузер для Agent с инструментами выбора элементов.**

**Как использовать:**
1. Browser встроен в редактор
2. Используй инструменты для выбора элементов
3. Передавай DOM информацию агенту
4. Browser layout и style editor (версия 2.2):
   - Дизайн и код одновременно
   - Новый браузерный сайдбар и дерево компонентов
   - Перемещение элементов, обновление цветов
   - Тестирование макетов и эксперименты с CSS в реальном времени
   - Мгновенное применение изменений к кодовой базе через агента

**Преимущества:**
- Работа с веб-приложениями напрямую
- Визуальное редактирование с применением к коду
- Тестирование изменений в реальном времени

📚 **Подробнее:** [cursor.com/changelog/2-0](https://cursor.com/changelog/2-0) | [cursor.com/changelog/2-2](https://cursor.com/changelog/2-2)

---

### Sandboxed Terminals (GA)
**Изолированные терминалы для агентов - по умолчанию на macOS и Linux.**

**Как работает:**
- Команды shell автоматически запускаются в sandbox
- Доступ только к workspace (read/write)
- Нет доступа к интернету
- Поддержка macOS (с версии 2.0) и Linux (с версии 2.3)

**Безопасность:**
- Команды, не в allowlist, автоматически изолируются
- Можно настроить доступ к git и сети на уровне команды (Enterprise)
- Защита от неавторизованного доступа к файлам и сети

📚 **Подробнее:** [cursor.com/changelog/2-0](https://cursor.com/changelog/2-0) | [cursor.com/changelog/2-3](https://cursor.com/changelog/2-3)

---

### Debug Mode
**Режим отладки с инструментированием runtime логами.**

**Как использовать:**
1. Включи Debug Mode в Agent
2. Cursor инструментирует приложение с runtime логами
3. Находит корневую причину багов
4. Работает через стеки, языки и модели

**Преимущества:**
- Воспроизведение и исправление самых сложных багов
- Автоматическое инструментирование
- Работа с любыми технологиями

📚 **Подробнее:** [cursor.com/changelog/2-2](https://cursor.com/changelog/2-2)

---

### AI Code Reviews
**AI код-ревью в редакторе с просмотром проблем в сайдпанеле.**

**Как использовать:**
1. AI Code Reviews автоматически анализирует изменения
2. Находит проблемы и показывает их в сайдпанеле
3. Исправляй баги прямо в Cursor
4. Дополняет Bugbot, который работает на GitHub, GitLab и других

**Преимущества:**
- Нахождение проблем до коммита
- Интеграция с редактором
- Работа с любыми системами контроля версий

📚 **Подробнее:** [cursor.com/changelog/2-1](https://cursor.com/changelog/2-1)

---

### Checkpoints (Контрольные точки)
**Автоматические снимки изменений Agent для отката.**

**Как использовать:**
1. Cursor автоматически создает контрольные точки при работе Agent
2. Используй контрольные точки для отката изменений
3. Полезно для экспериментов с Agent

**Преимущества:**
- Безопасное экспериментирование
- Легкий откат изменений
- История работы Agent

📚 **Подробнее:** [docs.cursor.com](https://docs.cursor.com)

---

### Voice Mode
**Голосовое управление Agent с преобразованием речи в текст.**

**Как использовать:**
1. Включи Voice Mode в настройках
2. Говори с Agent, используя встроенное преобразование речи в текст
3. Настрой кастомные ключевые слова для запуска агента

**Преимущества:**
- Удобство для голосового ввода
- Настраиваемые триггеры
- Интеграция с системой распознавания речи

📚 **Подробнее:** [cursor.com/changelog/2-0](https://cursor.com/changelog/2-0)

---

### Team Commands
**Командные команды и правила, управляемые через Cursor dashboard.**

**Как использовать:**
1. Определи кастомные команды и правила для команды в Cursor dashboard
2. Контекст автоматически применяется ко всем членам команды
3. Не нужно хранить файлы локально в редакторе
4. Централизованное управление админами

**Преимущества:**
- Единые стандарты для команды
- Централизованное управление
- Автоматическое применение правил

📚 **Подробнее:** [cursor.com/changelog/2-0](https://cursor.com/changelog/2-0)

---

### Layout Customization
**Настройка макетов с переключением ⌘+⌥+Tab.**

**Как использовать:**
1. Четыре макета по умолчанию: agent, editor, zen, browser
2. Используй ⌘+⌥+Tab для переключения между макетами
3. Используй Shift для движения назад (как в macOS)
4. Настрой макет по умолчанию для workspace

**Преимущества:**
- Быстрое переключение между режимами работы
- Настройка под свои предпочтения
- Удобная навигация между workspace

📚 **Подробнее:** [cursor.com/changelog/2-3](https://cursor.com/changelog/2-3)

---

### Instant Grep (Beta)
**Мгновенный grep для всех команд агента.**

**Особенности:**
- Все grep команды агента теперь мгновенные
- Поддерживается всеми моделями в Cursor
- Используется при ручном поиске в кодовой базе из сайдбара
- Поддержка regex и matching на word boundaries

**Преимущества:**
- Значительно быстрее поиск в кодовой базе
- Работает со всеми моделями
- Улучшенный UX поиска

📚 **Подробнее:** [cursor.com/changelog/2-1](https://cursor.com/changelog/2-1)

---

### Multi-agent Judging
**Автоматическая оценка результатов параллельных агентов.**

**Как работает:**
1. При запуске нескольких агентов параллельно
2. Cursor автоматически оценивает все результаты
3. Дает рекомендацию для лучшего решения
4. Выбранный агент имеет комментарий с объяснением выбора
5. Оценка происходит только после завершения всех параллельных агентов

**Преимущества:**
- Автоматический выбор лучшего решения
- Объяснение выбора
- Экономия времени на сравнение результатов

📚 **Подробнее:** [cursor.com/changelog/2-2](https://cursor.com/changelog/2-2)

---

### Pinned Chats
**Закрепление чатов в сайдбаре агента.**

**Как использовать:**
1. В сайдбаре агента закрепи важные чаты
2. Закрепленные чаты отображаются вверху
3. Быстрый доступ к важным разговорам

**Преимущества:**
- Организация важных разговоров
- Быстрый доступ к контексту
- Улучшенная навигация

📚 **Подробнее:** [cursor.com/changelog/2-2](https://cursor.com/changelog/2-2)

---

### AI Commit Message
**Автоматическая генерация сообщений коммитов.**

**Как использовать:**
1. Cursor анализирует подготовленные изменения
2. Учитывает историю репозитория
3. Следует соглашениям, таким как Conventional Commits
4. Генерирует сообщение коммита автоматически

**Преимущества:**
- Экономия времени на написание коммитов
- Следование стандартам
- Учет контекста проекта

📚 **Подробнее:** [docs.cursor.com](https://docs.cursor.com)

---

### Bugbot
**Автоматический анализ pull request'ов для выявления багов, уязвимостей и проблем с качеством кода.**

**Что делает Bugbot:**
- Анализирует изменения в pull request'ах
- Выявляет баги, уязвимости и проблемы с качеством кода
- Оставляет комментарии с объяснениями и предложениями по исправлению
- Работает автоматически на GitHub, GitLab и других системах контроля версий

**Как настроить:**
1. Перейди на [cursor.com/bugbot](https://cursor.com/bugbot)
2. Подключи свой репозиторий (GitHub, GitLab и т.д.)
3. Bugbot автоматически начнет анализировать pull request'ы
4. Получай комментарии с предложениями по улучшению кода

**Преимущества:**
- Автоматический анализ каждого PR
- Выявление проблем до мерджа
- Улучшение качества кода в команде
- Работает параллельно с AI Code Reviews в редакторе

**Отличие от AI Code Reviews:**
- **AI Code Reviews** - работает в редакторе Cursor перед коммитом
- **Bugbot** - работает автоматически на pull request'ах в репозитории

📚 **Подробнее:** [docs.cursor.com/ru/bugbot](https://docs.cursor.com/ru/bugbot)

---

### Автообновление документации через CLI
**Автоматическое обновление документации на основе изменений в коде с помощью Cursor CLI.**

**Что это:**
- Автоматическое обновление документации при изменении кода
- Интеграция с GitHub Actions для CI/CD
- Поддержание документации в актуальном состоянии

**Как настроить в GitHub Actions:**

**Вариант 1: Полностью автономный агент**
```yaml
name: Auto-update docs
on:
  push:
    branches: [main]
    paths:
      - 'src/**'

jobs:
  update-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Cursor CLI
        run: |
          curl -fsSL https://cursor.sh/install.sh | sh
      - name: Update documentation
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
        run: |
          cursor . --prompt "Обнови документацию на основе изменений в коде"
          git config user.name "Cursor Bot"
          git config user.email "bot@cursor.com"
          git add docs/
          git commit -m "docs: auto-update documentation" || exit 0
          git push
```

**Вариант 2: Детерминированный workflow**
```yaml
name: Auto-update docs
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  update-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Cursor CLI
        run: |
          curl -fsSL https://cursor.sh/install.sh | sh
      - name: Update specific docs
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
        run: |
          cursor . --prompt "Обнови README.md и API.md на основе изменений в src/"
```

**Преимущества:**
- Документация всегда актуальна
- Автоматизация процесса обновления
- Интеграция с CI/CD пайплайнами
- Экономия времени на поддержку документации

**Когда использовать:**
- Большие проекты с частыми изменениями
- Командная разработка
- Когда документация критически важна
- Для автоматизации процесса обновления

📚 **Подробнее:** [docs.cursor.com/ru/cli/cookbook/auto-update-docs](https://docs.cursor.com/ru/cli/cookbook/auto-update-docs)

---

### Каналы обновлений (Update Channels)
**Выбор между стабильными и предварительными версиями Cursor.**

**Доступные каналы:**

#### **Default (по умолчанию)**
- Стабильные релизы Cursor
- Рекомендуется для большинства пользователей
- Меньше багов и проблем
- Новые функции появляются позже

#### **Early Access**
- Предварительные версии с новыми функциями
- Доступ к функциям раньше других
- Может быть менее стабильным
- Для пользователей, которые хотят попробовать новое первыми

**Как переключиться между каналами:**

1. Открой Settings (`⌘+,`)
2. Перейди в раздел "Account" или "Updates"
3. Выбери канал обновлений:
   - **Default** - для стабильности
   - **Early Access** - для новых функций

**Рекомендации:**
- Для продакшн работы: **Default**
- Для экспериментов: **Early Access**
- Для команд: **Default** (по умолчанию для всех)

**Примечание:**
- Переключение между каналами требует перезапуска Cursor
- Ранние версии могут содержать баги
- Можно вернуться к Default в любой момент

📚 **Подробнее:** [docs.cursor.com/ru/account/update-access](https://docs.cursor.com/ru/account/update-access)

---

### Background Agent (Фоновые агенты)
**Запуск асинхронных процессов, которые редактируют и выполняют код в удалённой среде.**

**Что это:**
- Фоновые агенты работают асинхронно в удалённой среде
- Вы можете следить за их статусом
- Отправлять дополнительные команды в процессе работы
- В любой момент взять управление на себя

**Как использовать:**

**1. Активация режима фонового агента:**
- Нажми `Ctrl+E` (или `⌘+E` на macOS)
- Активируется режим фонового агента в интерфейсе
- Отправь запрос агенту
- Выбери агента из списка для просмотра статуса

**2. Боковая панель фонового агента:**
- Используй вкладку фонового агента в боковой панели
- Просматривай все агенты, связанные с аккаунтом
- Ищи существующие агенты
- Запускай новые фоновые агенты

**Настройка:**

**Требования:**
1. Подключение к GitHub
2. Предоставление прав на чтение и запись для репозиториев
3. Cursor версии 0.50 или выше

**Рабочая среда:**
- Изолированная среда на базе Ubuntu
- Доступ к интернету
- Возможность установки пакетов
- Безопасная изоляция от основной системы

**Преимущества:**
- Работа в фоновом режиме без отвлечения от основной задачи
- Асинхронное выполнение сложных задач
- Возможность запуска нескольких агентов одновременно
- Интеграция с Linear (версия 1.5+) - запуск агентов из Linear

**Когда использовать:**
- Длительные задачи рефакторинга
- Масштабные изменения в кодовой базе
- Задачи, требующие установки зависимостей
- Работа с несколькими задачами параллельно

**Примечание:**
- Для работы Background Agent требуется **Privacy Mode** (не Legacy)
- В режиме Privacy Mode (Legacy) фоновые агенты отключены

📚 **Подробнее:** [docs.cursor.com/ru/background-agent](https://docs.cursor.com/ru/background-agent)

---

### Analytics V2 (для командных аккаунтов)
**Новая инфраструктура аналитики для командных аккаунтов.**

**Что это:**
С 1 сентября 2025 года Cursor внедрил обновлённую аналитическую инфраструктуру Analytics V2 для командных аккаунтов.

**Что отслеживается:**
- Общее число принятых строк кода
- Общее число предложенных строк кода
- Общее число принятых вкладок (tabs)
- Метрики использования AI функций

**Кто использует:**
- Пользователи Cursor версии 1.5 и выше
- Автоматическое переключение на V2
- Для командных аккаунтов и организаций

**Преимущества:**
- Более точное отслеживание метрик
- Улучшенная инфраструктура
- Детальная аналитика использования
- Полезно для оценки эффективности команды

**Когда использовать:**
- Для командных аккаунтов
- Для отслеживания продуктивности команды
- Для оценки использования AI функций
- Для анализа принятия предложений кода

📚 **Подробнее:** [docs.cursor.com/ru/account/teams/analyticsV2](https://docs.cursor.com/ru/account/teams/analyticsV2)

---

### Интеграция с GitHub Actions
**Использование Cursor CLI в GitHub Actions и других системах CI/CD.**

**Что это:**
Cursor CLI можно интегрировать с GitHub Actions для автоматизации задач разработки в CI/CD пайплайнах.

**Базовая настройка:**

```yaml
name: Cursor CI Integration
on:
  pull_request:
    types: [opened, synchronize]
  push:
    branches: [main]

jobs:
  cursor-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Установка Cursor CLI
        run: |
          curl https://cursor.com/install -fsS | bash
          echo "$HOME/.cursor/bin" >> $GITHUB_PATH
      
      - name: Запуск Cursor Agent
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
        run: |
          cursor-agent -p "Проверь код и исправь проблемы линтера" --model gpt-4
```

**Практические сценарии:**

**1. Автоматическое исправление проблем линтера:**
```yaml
- name: Fix linting issues
  env:
    CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
  run: |
    cursor-agent -p "Исправь все проблемы ESLint в измененных файлах" --model gpt-4
    git config user.name "Cursor Bot"
    git config user.email "bot@cursor.com"
    git add .
    git commit -m "fix: auto-fix linting issues" || exit 0
    git push
```

**2. Обновление тестов:**
```yaml
- name: Update tests
  env:
    CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
  run: |
    cursor-agent -p "Обнови тесты для измененных компонентов" --model gpt-4
```

**3. Проверка безопасности:**
```yaml
- name: Security check
  env:
    CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
  run: |
    cursor-agent -p "Проверь код на уязвимости безопасности и предложи исправления" --model gpt-4
```

**4. Генерация changelog:**
```yaml
- name: Generate changelog
  env:
    CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
  run: |
    cursor-agent -p "Обнови CHANGELOG.md на основе коммитов" --model gpt-4
```

**Уровни автономности агента:**

**Полная автономность:**
- Агент самостоятельно принимает все решения
- Автоматическое применение изменений
- Подходит для простых задач

**Детерминированный workflow:**
- Агент изменяет только указанные файлы
- Требуется проверка перед применением
- Подходит для критичных задач

**Настройка API ключа:**

1. Создай API ключ в настройках Cursor
2. Добавь его в GitHub Secrets:
   - Перейди в Settings → Secrets → Actions
   - Создай новый секрет `CURSOR_API_KEY`
   - Вставь свой API ключ

**Преимущества:**
- Автоматизация рутинных задач
- Проверка и исправление кода в CI
- Обновление документации и тестов
- Интеграция AI в процесс разработки

**Когда использовать:**
- Для автоматизации code review
- Для автоматического исправления проблем
- Для обновления документации
- Для генерации changelog и release notes

📚 **Подробнее:** [docs.cursor.com/ru/cli/github-actions](https://docs.cursor.com/ru/cli/github-actions)

---

## 🎯 Best Practices

### Общие принципы
- **Начинай с простого** — не усложняй настройку
- **Итеративно улучшай** — добавляй правила по мере необходимости
- **Тестируй настройки** — проверяй работу всех функций
- **Документируй изменения** — веди changelog настроек

### Оптимизация производительности
- Используй `.cursorignore` для исключения ненужных файлов
- Настраивай автоконтекст для больших проектов
- Регулярно обновляй Project Rules
- Мониторь использование токенов

### Командная работа
- Делитесь Project Rules через репозиторий
- Создавайте общие Notepads для команды
- Настраивайте единые стандарты кода
- Используйте Agent Mode для командных задач

### Клонирование проектов
- Создавайте шаблоны для разных типов проектов
- Используйте клонирование для быстрого старта
- Адаптируйте настройки под конкретные задачи
- Сохраняйте лучшие практики в шаблонах

---

## 🚀 Готово!

После выполнения всех шагов ваш проект будет полностью настроен для эффективной работы с Cursor. AI будет понимать контекст проекта, следовать установленным правилам и помогать в разработке согласно best practices.

**Следующие шаги:**
1. Начни разработку с использованием настроенного Cursor
2. По мере необходимости добавляй новые правила в Project Rules
3. Обновляй Notepads с новой информацией о проекте
4. Регулярно проверяй актуальность документации

---

**Cursor теперь настроен как ваш персональный AI-эксперт по разработке!** 🎉 